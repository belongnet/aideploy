{
  "name": "aideploy-command-builder",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "npm run test && npm run build"
  }
}
